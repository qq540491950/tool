# Shell scripts must use LF for bash compatibility
*.sh text eol=lf

# Batch scripts must use CRLF for cmd.exe compatibility
*.bat text eol=crlf

# Shared package list — LF so both bash read and bat for/f can parse it
packages.txt text eol=lf
