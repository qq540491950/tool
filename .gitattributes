# Shell scripts must use LF for bash compatibility
*.sh text eol=lf

# Batch scripts must use CRLF for cmd.exe compatibility
*.bat text eol=crlf

# PowerShell scripts and data files — LF for cross-editor consistency
*.ps1  text eol=lf
*.psd1 text eol=lf
